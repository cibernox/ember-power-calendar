<p>Choose the dates you prefer in the order you prefer them:</p>

{{power-calendar
  multiple=true
  displayedMonth=displayedMonth
  selected=preferredDates
  onChange=(action (mut preferredDates) value="moment")}}

Preferred dates:
<ol>
  {{#each preferredDates as |d|}}
    <li>{{moment-format d 'LLL'}}</li>
  {{/each}}
</ol>