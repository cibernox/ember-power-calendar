<PowerCalendar
  @center={{center}}
  @selected={{selected}}
  @onSelect={{action (mut selected) value="date"}} as |calendar|>
  <calendar.Nav/>
  <calendar.Days @disabledDates={{disabledDates}}/>
</PowerCalendar>