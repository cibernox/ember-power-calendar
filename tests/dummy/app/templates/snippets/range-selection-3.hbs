<PowerCalendarRange
  @center={{center}}
  @selected={{mediumRange}}
  @minRange={{3}}
  @onCenterChange={{action (mut center) value="date"}}
  @onSelect={{action (mut mediumRange) value="date"}} as |cal|>
  <cal.Nav/>
  <cal.Days/>
</PowerCalendarRange>
