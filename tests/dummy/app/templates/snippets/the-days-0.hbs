<PowerCalendarMultiple @selected={{selectedDays}} @onSelect={{action (mut selectedDays) value="date"}} as |calendar|>
  <calendar.nav/>
  <calendar.days class="custom-day-demo-calendar" @dayClass={{this.customClass}}/>
</PowerCalendarMultiple>