<PowerCalendarRange
  @center={{this.center}}
  @selected={{this.mediumRange}}
  @minRange={{3}}
  @onCenterChange={{action (mut this.center) value="date"}}
  @onSelect={{action (mut this.mediumRange) value="date"}} as |cal|>
  <cal.Nav/>
  <cal.Days/>
</PowerCalendarRange>
