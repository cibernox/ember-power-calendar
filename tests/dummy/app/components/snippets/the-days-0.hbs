<PowerCalendarMultiple @selected={{this.selectedDays}} @onSelect={{action (mut this.selectedDays) value="date"}} as |calendar|>
  <calendar.Nav/>
  <calendar.Days class="custom-day-demo-calendar" @dayClass={{this.customClass}}/>
</PowerCalendarMultiple>