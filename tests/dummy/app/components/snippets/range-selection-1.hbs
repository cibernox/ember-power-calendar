<PowerCalendarRange
  @center={{this.center}}
  @selected={{this.range}}
  @onSelect={{action (mut this.range) value="date"}} as |calendar|>
  <calendar.Nav/>
  <calendar.Days/>
</PowerCalendarRange>
