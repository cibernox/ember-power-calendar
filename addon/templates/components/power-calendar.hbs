{{yield (hash
  displayedMonth=(readonly currentlyDisplayedMonth)
  selected=(readonly selected)
  nav=(component navComponent
    calendar=(readonly publicAPI)
    month=(readonly currentlyDisplayedMonth)
    onMonthChange=(readonly onMonthChange)
  )
  days=(component daysComponent
    calendar=(readonly publicAPI)
    onChange=(readonly onChange)
  )
  actions=publicAPI.actions
)}}
